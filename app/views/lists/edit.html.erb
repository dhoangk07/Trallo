<h1> Edit list</h1>

<%= form_for @list do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

<%= link_to 'Edit', edit_list_path(@list) %>
 <%= link_to 'Destroy', list_path(@list), 'data-confirm' => 'Are you sure?' ,method: :delete %>

  <%= f.submit %>

<% end %>