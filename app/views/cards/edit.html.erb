<h1> Edit card</h1>

<%= form_for [@list, @card], html: { multipart: true } do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

   <p>

   <%= f.file_field :file %>

  </p> 

  <%= f.submit %>

<% end %>
